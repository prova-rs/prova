-- Lives under `proofs/`, NOT at the home. Reachable as `require("shared.thing")` only when the
-- require-root is configured to `proofs/` — the whole point of the configurable root.
return { value = 7 }
