---
description: "Basic prova init fixture — one prompt with a default, flat render"
authors: ["prova test suite"]

requires:
  archetect: "3.0.0"

templating:
  trim_blocks: true
  lstrip_blocks: true
