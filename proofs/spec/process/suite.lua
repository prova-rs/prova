-- The process transport's interpose posture: shell.proxy, the PATH shim. Completes the
-- Mock/Proxy/Driver triad for `process` (shell.run/spawn already originate). Tier A, spec-first.
suite.config{ name = "spec-process" }
