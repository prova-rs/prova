-- The process transport's interpose posture: shell.proxy, the journaling PATH shim.
-- IMPLEMENTED and graduated; flag-free guardrails. Completes the Mock/Proxy/Driver triad
-- for `process` (shell.run/spawn originate; terminal.mock shadows interactive CLIs).
suite.config{ name = "spec-process" }
