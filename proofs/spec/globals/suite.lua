-- The §2 injection contract (docs/plans/api-freeze.md): reserved names, write-protected
-- globals, require-able bundled namespaces, [run] globals exclusion. IMPLEMENTED and
-- graduated; these run flag-free as guardrails on every silent-collision path.
suite.config{ name = "spec-globals" }
