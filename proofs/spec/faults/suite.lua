-- The fault vocabulary (docs/design/mocks-proxies-drivers.md): latency/drop/corrupt/
-- throttle/after on the proxy substrate — toxiproxy-in-process, no extra daemon.
-- IMPLEMENTED and graduated; flag-free guardrails.
suite.config{ name = "spec-faults" }
