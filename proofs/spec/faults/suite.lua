-- The fault vocabulary (docs/design/mocks-proxies-drivers.md): latency/drop/corrupt/throttle/
-- after on the proxy substrate — toxiproxy-in-process, no extra daemon. Tier A, spec-first.
suite.config{ name = "spec-faults" }
