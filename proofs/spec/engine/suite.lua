-- The promise ENGINE's own black-box surface. The feature was bootstrapped "implemented first,
-- promised by hand" (docs/plans/api-freeze.md) — this suite holds its standing guardrails: the
-- `promises` flag and its open-promise outcome, `--promises --list`, the `prova promises` /
-- `prova burndown` verbs, and the learn topic that teaches them.
suite.config{ name = "promise-engine" }
