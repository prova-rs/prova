-- The unified socket transport (docs/design/mocks-proxies-drivers.md, Tier A). The L4 substrate
-- every stream transport shares: scheme-unified addressing, three postures, framing strategies.
suite.config{ name = "spec-socket" }
