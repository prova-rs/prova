-- THE SPEC for the utility belt (docs/plans/api-freeze.md §1): tiny native-required primitives —
-- base64, hash, uuid, url — that no pure-Lua plugin can provide for itself. Same tech-first
-- module grammar as the formats.
suite.config{ name = "spec-utilities", spec = "api-freeze §1 — utility belt" }
