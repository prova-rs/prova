-- The websocket transport: the http upgrade path, full-duplex, message-framed by the
-- protocol itself. websocket_test.lua holds the graduated mock/driver guardrails (flag-free);
-- proxy_test.lua stages the interpose posture (2026-07-27) — the last cell in the transport
-- matrix: wiretap transcripts + the fault vocabulary. ws cassettes wait on the
-- scripted-conversation format design.
suite.config{ name = "spec-websocket" }
