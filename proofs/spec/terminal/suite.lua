-- The terminal (pty) transport (docs/design/mocks-proxies-drivers.md worked example): the
-- Driver-primary kernel transport for TUI/CLI proof — pty alloc + screen model + expect.
-- The differentiated surface: no adjacent tool has it. Tier A, spec-first.
suite.config{ name = "spec-terminal" }
