[package]
name = "env"
description = "This project's own capability predicates (docs/design/capabilities.md). A capability declared `{ package = \"env\", capability = \"<name>\" }` resolves to `capabilities.<name>` here — the namespace IS the advertisement."
