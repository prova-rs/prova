[package]
name = "{{ project_name }}"
version = "0.1.0"
edition = "2021"
description = "{{ description }}"

# Dependency-free on purpose: this fixture must `cargo build` offline in prova's acceptance tests.
[dependencies]
