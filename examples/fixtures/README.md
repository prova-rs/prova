# Example fixtures

These fixture projects are deliberately plain, self-contained apps used by the
runnable examples. They may eventually graduate into proper archetypes under the
Archetect organizations, at which point the examples render them instead of
checking them in.
